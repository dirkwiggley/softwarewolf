Whenever adding or modifying rules inside the globals.css file moving forward,
  keep these three key principles in mind to prevent styles from breaking or striking 
  each other out:
1. The Rule of Isolation for Global Selectors (*, html, body)
  The Rule: Never write loose global element selectors (like *, div, p, or input) 
    directly in the file root.
  Why: Raw global element selectors have blanket authority in standard CSS. They will instantly 
    override Tailwind's internal compiled layout utilities.
  What to do: If you must reset general element behaviors, always wrap them inside a native layer
    or use Tailwind's native @utility macro block to keep them safely partitioned at the lowest 
    baseline priority layer.
2. The Tailwind v4 Top-to-Bottom Processing Order
  Tailwind v4 compiles styles based on a strict cascading layout order from the top of your file
    to the bottom:
  1. Directives & Configurations: @import "tailwindcss"; and @source paths must stay at the absolute 
    top to initialize the engine.
  2. Base Rules / Layer Overrides: General CSS reset blocks.
  3. Custom Components & Utilities: Explicit class rules (like .wolf-panel) must stay at the 
    absolute bottom of the file. This ensures they load last and can naturally cascade over any 
    global resets written above them.
3. Choose the Right Selector Strategy
  When adding your own styles to globals.css, choose your syntax based on how you want the class to
    behave:
    Use .your-class-name (Standard CSS Block): Use this for structured components (like cards, layout
      grids, or complex forms) where you are mixing standard properties (padding: 40px;) with a Tailwind
      @apply string. This forces the browser to evaluate it as an absolute rule down the cascade.
    Use @utility your-utility-name (Tailwind v4 Macro): Use this if you are creating a quick utility 
      shortcut that relies entirely on Tailwind @apply classes 
      (e.g., @utility btn-blue { @apply bg-blue-500 text-white rounded; }). 
      This registers it natively within Tailwind's internal compilation layers.

Cascade Rules for New ExtensionsNo Local CSS Customizations: 
  Do not write independent CSS configurations inside local modules. If a page requires a new layout 
  utility or structural variation, add it directly to @softwarewolf/ui/index.css.CentralizedSecurity
  Enforcement: Sub-routes (e.g., /docs/api) must either inherit a parent folder layout.tsx running 
  PageGuard or declare explicit guard lines individually to avoid accidentally leaking internal admin
  routes.

Architectural Standards for New PagesEvery new route folder created within apps/web/app/ must strictly
follow a three-tier design layout to prevent design regression and maintain structural uniformity.
┌──────────────────────────────────────────────────────────┐
│ Main Layout Shell                                        │
│ ┌──────────────────────────────────────────────────────┐ │
│ │ <PageGuard allowedRoles={[...]} fallbackUrl="/home"> │ │
│ │ ┌──────────────────────────────────────────────────┐ │ │
│ │ │ Container Panel (.wolf-panel)                    │ │ │
│ │ │ (Centered Layout, 40px Padding, Theme-Aware)    │ │ │
│ │ └──────────────────────────────────────────────────┘ │ │
│ └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
Security Layer: Wrap page compositions or sub-layouts in your authorization engine to intercept 
  unauthorized traffic early in the rendering lifecycle.
Layout Consistency: Restrict content width using your shared container tokens to match the 
  high-density tables and control matrices of your core pages.
Cascading Classes: Utilize Tailwind v4 native CSS variables (--color-wolf-*) for custom theme 
  variations instead of hardcoded utility strings.

CURRENT FILE ARCHITECTURE STATE:
1. WORKSPACE LAYOUT & TECH STACK:
   - Structure: npm workspaces monorepo with `apps/web` (Next.js), `apps/docs` (Express microservice), and `packages/ui` (shared design configs).
   - Styling: Tailwind v4 compilation engine using global stylesheets (`apps/web/app/globals.css` and `packages/ui/index.css`).
   - Database: MariaDB managed via Prisma 7 using a native JS Driver Adapter in `apps/docs/src/db.ts`.
2. Sass could not be used due to an overwhelming number of conflicts.
3. apps/docs/prisma/schema.prisma: Updated 'User' table with a 'password' field (String @default("")).
4. apps/docs/src/utils/jwt.ts: Exposes 'signToken' and 'verifyToken' utilities encoding userId, role, username, displayName, and email into a 24-hour expiration token payload.
5. apps/docs/src/middlewares/auth.ts: Intercepts 'auth_token' cookie. If missing or invalid, attaches an anonymous guest fallback request profile ({ id: null, role: 'GUEST', displayName: 'Anonymous Guest' }). Otherwise, decodes the true JWT data. Uses an Express global type extension for 'req.user'.
6. apps/docs/src/controllers/system.ts: All administrative database CRUD functions fully written, casting 'req' to standard 'Request' type (relying on the global extension). Includes self-deletion guards for the active Admin.
7. apps/web/app/SecurityContext.tsx: Client context rehydrating profile states dynamically from GET /api/system/auth/me. Exposes loginUser(username) and logoutUser().
8. apps/web/app/PageGuard.tsx: Enforces strict route redirection. If a user is not logged in AND does not have 'sessionStorage.getItem("wolf_guest_allowed") === "true"', they are pushed instantly to /login. Guests trying to access non-guest paths are intercepted here.
9. apps/web/app/page.tsx: minimal page that looks at a user's role. If they have ADMIN or MANAGER then they are sent to admin-hub, otherwise they go to home

THE devIndicator SHOWING IN DEV mode
This is an overlay that is added by Next.js in dev mode. It gives you context about the active route such as Route State, Bundler Tracking, Build Activity
If that overlay is getting in the way you can move or hide it by updating the apps/web/next.config.ts file:

const nextConfig = {
  devIndicators: {
    position: 'bottom-right', // Options: 'bottom-left' | 'bottom-right' | 'top-left' | 'top-right'
  },
}

or to disable it completely:

const nextConfig = {
  devIndicators: false, // Prevents the N icon from rendering at all, but still logs console errors
}

